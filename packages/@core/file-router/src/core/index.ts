export { PAGE_DEGREE_SPLITTER, PATH_SPLITTER } from './constants';
export { default } from './core';

export { splitRouterName, transformRouterNameToPath } from './core/transform';

export {
  type ElegantRouterFile,
  type ElegantRouterNamePathEntry,
  type ElegantRouterNamePathMap,
  type ElegantRouterOption,
  type ElegantRouterTree,
} from './types';
