import { Block } from '../../blocks';
import { Layout } from '../../blocks/layout';

export const PreferencesLayout = () => {
  return (
    <>
      <Block title="布局">
        <Layout />
      </Block>
    </>
  );
};
